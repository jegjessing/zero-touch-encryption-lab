{
  "name": "zero-touch-encryption-lab",
  "version": "1.0.0",
  "description": "Root tooling for the Zerotouch lab — formatting, linting and running the service test suites.",
  "type": "module",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "npm test --prefix services/classifier && npm test --prefix services/message-api"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "eslint": "^9.14.0",
    "globals": "^15.12.0",
    "prettier": "^3.3.3"
  }
}
