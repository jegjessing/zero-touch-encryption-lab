{
  "name": "zerotouch-message-api",
  "version": "1.0.0",
  "description": "Accepts messages, classifies them, encrypts sensitive ones at rest, and stores them in Postgres.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.11.1"
  }
}
